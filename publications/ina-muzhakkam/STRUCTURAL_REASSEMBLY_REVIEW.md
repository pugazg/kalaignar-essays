# Structural reassembly review — இன முழக்கம்

Source: `TVA_BOK_0063958_இன_முழக்கம்.pdf`  
User input: full 50-scan word-to-word transcription  
Review scope: **structure / punctuation / spacing / paragraph order / page placement / headings / poetry lineation / non-source noise only**

## Controlling rule for this pass

The user's supplied words are retained as the lexical baseline. This review does **not** silently substitute alternate scan-appearing spellings or modernised forms. The source scan controls where those words belong and how the printed structure is represented.

User-authorised corrections that supersede an earlier staging decision are recorded in [`transcription-intake/USER_CORRECTIONS.md`](transcription-intake/USER_CORRECTIONS.md).

## Front-matter cleanup

- scan 1: handwriting/OCR garbage such as `சென்டா`, `Darya Same`, `Tin.`, `GOPERIYAS WAMY`, `Periya Same` is excluded from printed text and treated as physical-copy/OCR noise;
- scan 2: `K. N. சங்கரன்...` ownership/library stamps are excluded from printed text; title/author/seller layout is restored;
- scan 3: `முதற் பதிப்பு: செப்டம்பர் 1951` and `விலை அணா 0-8-0` belong here, not scan 2; stray `9.` is removed; ownership-stamp text is excluded;
- scan 4: the six-entry `உள்ளுறை` is restored as a contents list; stamp text is excluded;
- scan 5: `பதிப்புரை` is restored as paragraphs ending in `பண்ணையாளர்`; ownership-stamp text is excluded.

## `இன முழக்கம்` structural corrections

- scan 7: `மாதவி.` belongs after the first `காரணம் என்ன?` and **before** the `* * *` separator; `கண்ணகி.` belongs after the second `காரணம் என்ன?`; transcript hyphen/list artefacts are removed;
- scan 8: `இளங்கோவடிகள்.` follows `காரணம் என்ன?`; the source ornament is `* * *`, not a stray hyphen/star fragment;
- scan 9: `புலவர் பிசிராந்தையார்.` follows its `காரணம் என்ன?`; source separator restored before the `வடலூர் இராமலிங்கம்` unit;
- scan 10: user correction establishes that `விசயர்` is **misplaced, not stray**. It belongs after `கர்வத்தால் கனத்துப்போன கனக`, giving the source-supported sequence `கர்வத்தால் கனத்துப்போன கனக விசயர் தலைகளில் கல்லேற்றி...`; the earlier removal decision is superseded. `வடலூர் இராமலிங்கம்.` and `சேரன் செங்குட்டுவன்.` remain restored as speaker/subject labels in their source positions;
- scan 11: `திருவள்ளுவர்` and `பாரிவள்ளல்.` labels are separated from the `* * *` ornaments;
- scans 12–13: self-respect-speaker dialogue is paragraphised and quotation punctuation restored from the scan; no supplied lexical token is normalised.

## `சொர்க்க லோகத்தில்` structural corrections

- scans 14–15: the opening celestial-scene description is divided into the source's successive action paragraphs rather than one OCR paragraph;
- scans 16–19: each quoted testimony is paired with its source-visible speaker label (`கண்ணப்ப நாயனார்`, `சிறுத்தொண்டர்`, `நந்தனார்`, `காரைக்கால் அம்மையார்`, `அப்பர்`, `இயற்பகை நாயனார்`, `மூர்த்தி நாயனார்`, `எறிபத்த நாயனார்`, `கோட்புலி நாயனார்`);
- scans 20–23: dialogue/parenthetical stage-like interruptions are separated correctly; quotation scope is restored structurally without rewriting words;
- scan 22: stray trailing `2` from the supplied transcript is removed as non-source OCR noise;
- scan 24: the article closes before the ornament; the subsequent `திராவிட சம்பத்து` promotion is preserved as separate promotional matter.

## `முரசறைவாய்` structural corrections

- prose paragraphs and direct-address passages are separated according to the scan;
- page-break word fragments remain page-boundary fragments rather than being duplicated;
- scan 29 closes with `போர் முரசறைவாய்` before the source ornament.

## `பழிக்குப் பழி` structural corrections

- OCR-run-together punctuation/spacing is separated without changing word tokens;
- quotation blocks and rhetorical paragraphs are restored;
- **important page correction:** the final lines `பழி வாங்க வேண்டாமா? மிஞ்சியுள்ள தலைவர்கள் ... திராவிடரே? சொல்லுங்கள்!` belong to scan **37 / printed 36**, not scan 36 / printed 35;
- scan 37 then closes the article, followed by a separate two-novel advertisement.

## `ஆரியம் பேசுகிறது` structural corrections

- scan 38 ends at the source continuation `குலச்`;
- scan 39 begins with its continuation and closes the article before the ornament;
- no prose from scan 40 is imported into this article.

## Poetry-front-matter / poetry corrections

- scan 40 is separated into `கவிதைகளைப் பற்றி` (author note) and `மதிப்புரை` (review), with `மு. கருணாநிதி.` and `இரா. நெடுஞ்செழியன்.` retained as source credits;
- scans 41–49 are restored as verse with source lineation rather than flattened prose;
- **page correction:** heading `வா!` belongs at the top of scan **43 / printed 42**, not the end of scan 42;
- **page correction:** heading `யோசித்துப் பார்!` belongs at the top of scan **44 / printed 43**, not the end of scan 43;
- headings inside the poetry section are kept as separate source-visible poem headings, while the contents witness remains the single section label `கவிதைகள்`.

## Catalogue cleanup

- scan 50 is represented as a catalogue/advertisement table rather than running prose;
- title / author / price columns are separated structurally;
- no catalogue entry is imported into body text.

## Lexical non-regression

This pass intentionally does **not** resolve lexical scan/baseline disagreements. Any such difference discovered while laying out the pages must be recorded during P2/P5 and requires source/user-authorised handling; it must not be silently corrected here.

The scan-10 `விசயர்` correction is not a lexical substitution: the user-supplied token is retained and only its source-supported position is corrected.

## Result

- publication identified: **PASS**
- 50-scan physical order mapped: **PASS**
- major section/article boundaries: **PASS / structurally established**
- supplied transcription structural reassembly: **COMPLETE as staging baseline, with `USER_CORRECTIONS.md` override applied during P2 transfer**
- canonical per-scan P2 records: **NEXT**
- strict visual word/punctuation fidelity P5: **NOT STARTED**