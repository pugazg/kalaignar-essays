# P3 Article Assembly Review — ஆறுமாதக் கடுங்காவல்

## Gate result

**P3 — COMPLETE / PASS — 3/3 article structures assembled and rechecked against canonical P2 records.**

The user corrected the publication structure during P3. The correct article inventory is:

| No. | Article | Structural source range | Assembly |
|---:|---|---|---|
| 1 | **முரசு** | body scans **10–65**; scan 65 closes with the triple `அதிர்ந்தது முரசு` refrain and printed star | `articles/01-murasu.md` |
| 2 | **களம்** | scan **66** illustrated `தடை / கல்லக்குடி` transition; scan 67 blank/reverse; heading **களம்** on scan **68**; body continues through scan **106** | `articles/02-kalam.md` |
| 3 | **சிறை** | scan **108** illustrated `சிறை` intertitle; scan 109 blank/reverse; body scans **110–223** | `articles/03-sirai.md` |

## Boundary evidence

### 1. முரசு

- scan 10 begins the `முரசு` body;
- scan 65 ends with:
  - `அதிர்ந்தது முரசு!`
  - `அதிர்ந்தது முரசு!!`
  - `அதிர்ந்தது முரசு!!!`
  - printed star;
- therefore `முரசு` closes at scan **65**.

### 2. களம்

- scan 66 is an illustrated transition carrying `தடை` and station-board `கல்லக்குடி`;
- scan 67 is blank/reverse;
- scan 68 carries the source heading **`களம்`** and starts the body;
- scan 69 is the next page with visible printed folio **66**;
- scans 68–106 form the continuous `களம்` body;
- scan 106 closes the body; scan 107 is blank/reverse.

This preserves the user's correction that the page-66 transition belongs to **களம்**, not `முரசு`.

### 3. சிறை

- scan 108 is the illustrated `சிறை` intertitle;
- scan 109 is blank/reverse;
- scan 110 repeats `சிறை` and begins continuous body text;
- embedded source headings inside the article, including `ஜனநாயகம்`, `மொழிப்பற்று — பித்தா ?`, `அதிசயம்!`, `ஹிட்லர் பாட்டு - சுதந்திர மெட்டு!`, `சிறை திறந்தது`, and `வெற்றி! வெற்றி!!`, are preserved;
- scan 223 is the final text-bearing page and work terminus.

## Assembly fidelity

- all three files are assembled only from canonical P2 page records;
- no source wording was normalized during assembly;
- the user-confirmed P2 resolutions `ஆகிவிடக்கூடிய` and `அடைந்தார்` are carried into the reading copies;
- `03-sirai.md` was rechecked scan-by-scan against canonical records after the structural correction; assembly-only omissions on scans 127, 159–160, 166–169, 171, and 174 were repaired, along with embedded-heading truncations on scans 190, 194, 214, and 218;
- no unresolved `[…]` reading remains;
- P3 blockers — **0**.

## Exact next activity

**P4 — source audit / completeness review.**

Verify that the three article assemblies, canonical page map, source metadata, front/back matter accounting, page-boundary coverage and publication-level controls are mutually consistent before starting P5.
